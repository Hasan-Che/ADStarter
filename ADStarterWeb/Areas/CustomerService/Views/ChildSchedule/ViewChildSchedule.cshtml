@model IEnumerable<ADStarter.Models.ViewModels.ChildScheduleViewModel>

@{
    ViewData["Title"] = "View Child Schedule";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="page-body">
    <div class="container-fluid">
        <div class="page-title">
            <h3>Child Schedule</h3>
        </div>
    </div>

    <div class="container mt-4">
        <div class="card">
            <div class="card-header">
                <h5>Schedule</h5>
            </div>
            <div class="card-body">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>Child Name</th>
                            <th>Therapist Name</th>
                            <th>Session Date</th>
                            <th>Slot Time</th>
                            <th>Program Name</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.ChildName</td>
                                <td>@item.TherapistName</td>
                                <td>@item.SessionDate.ToString("yyyy-MM-dd")</td>
                                <td>@item.SlotTime</td>
                                <td>@item.ProgramName</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
