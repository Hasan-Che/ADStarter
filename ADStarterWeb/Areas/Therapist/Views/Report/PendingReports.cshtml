@model List<ADStarter.Models.ViewModels.PendingReportViewModel>

@{
    ViewData["Title"] = "Pending Reports";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="page-body">
    <div class="container-fluid">
        <div class="page-title">
            <h3>Pending Reports</h3>
        </div>
    </div>

    <div class="container mt-4">
        <div class="card">
            <div class="card-body">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>Child Name</th>
                            <th>Date of Report</th>
                            <th>Program Name</th>
                            <th>Slot Time</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.ChildName</td>
                                <td>@item.ReportDateTime.ToString("yyyy-MM-dd")</td>
                                <td>@item.ProgramName</td>
                                <td>@item.SlotTime</td>
                                <td>
                                    <a class="btn btn-success" href="@Url.Action("Update", "Report", new { area = "Therapist", scheduleId = item.ScheduleId })"><i class="fa fa-upload"></i> Update Report</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>