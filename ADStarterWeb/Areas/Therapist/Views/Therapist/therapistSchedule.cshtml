@* @model List<ADStarter.Models.ViewModels.TherapistScheduleViewModel>

@{
    ViewData["Title"] = "Therapist Schedule";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="container">
    <h1>Therapist Schedule</h1>
    <table class="table">
        <thead>
            <tr>
                <th>Child Name</th>
                <th>Session Date Time</th>
                <th>Slot Time</th>
                <th>Program</th>
                <th>Slot Price</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var schedule in Model)
            {
                <tr>
                    <td>@schedule.ChildName</td>
                    <td>@schedule.SessionDateTime.ToString("yyyy-MM-dd HH:mm")</td>
                    <td>@schedule.SlotTime</td>
                    <td>@schedule.ProgramName</td>
                    <td>@schedule.SlotPrice</td>
                </tr>
            }
        </tbody>
    </table>
</div>
 *@

 @model List<ADStarter.Models.ViewModels.TherapistScheduleViewModel>

@{
    ViewData["Title"] = "Therapist Schedule";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="page-body">
    <div class="container-fluid">
        <div class="page-title">
            <div class="row">
                <div class="col-6">
                    <h3>Therapist Schedule</h3>
                </div>
                <div class="col-6">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item">
                            <a asp-controller="Dashboard" asp-action="Index">
                                <svg class="stroke-icon">
                                    <use href="../assets/svg/icon-sprite.svg#stroke-home"></use>
                                </svg>
                            </a>
                        </li>
                        <li class="breadcrumb-item">Program Controls</li>
                        <li class="breadcrumb-item active">Therapist Schedule</li>
                    </ol>
                </div>
            </div>
        </div>
    </div>
    <!-- Container-fluid starts-->
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-12">
                <div class="card">
                    <div class="card-header">
                        <h5>Therapist Schedule</h5>
                        <span>View therapist schedules below.</span>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table table-striped table-bordered">
                                <thead>
                                    <tr>
                                        <th>Child Name</th>
                                        <th>Session Date Time</th>
                                        <th>Slot Time</th>
                                        <th>Program</th>
                                        <th>Slot Price</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var schedule in Model)
                                    {
                                        <tr>
                                            <td>@schedule.ChildName</td>
                                            <td>@schedule.SessionDateTime.ToString("yyyy-MM-dd HH:mm")</td>
                                            <td>@schedule.SlotTime</td>
                                            <td>@schedule.ProgramName</td>
                                            <td>@schedule.SlotPrice</td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Container-fluid Ends-->
</div>

@section Script {
    <script>
        // Optional: Add any scripts specific to this view
    </script>
}
