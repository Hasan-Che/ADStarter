@* @model ADStarter.Models.ViewModels.ScheduleViewModel

@{
    ViewData["Title"] = "Delete Schedule";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="page-body">
    <div class="container-fluid">
        <div class="page-title">
            <div class="row">
                <div class="col-6">
                    <h3>Delete Schedule</h3>
                </div>
                <div class="col-6">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item">
                            <a asp-controller="Dashboard" asp-action="Index">
                                <svg class="stroke-icon">
                                    <use href="~/assets/svg/icon-sprite.svg#stroke-home"></use>
                                </svg>
                            </a>
                        </li>
                        <li class="breadcrumb-item active">Delete Schedule</li>
                    </ol>
                </div>
            </div>
        </div>
    </div>

    <div class="container-fluid">
        <div class="card mb-4">
            <div class="card-body">
                <h4>Are you sure you want to delete this schedule?</h4>
                <div>
                    <h5>@Model.SessionDate.ToShortDateString() - @Model.SlotTime</h5>
                    <h5>@Model.ProgramName</h5>
                    <h5>@Model.TherapistName</h5>
                </div>
                <form asp-action="DeleteSchedule" method="post">
                    <input type="hidden" asp-for="Session_ID" />
                    <button type="submit" class="btn btn-danger">Delete</button>
                    <a asp-action="ViewSchedule" asp-route-childId="@Model.Child_ID" class="btn btn-secondary">Cancel</a>
                </form>
            </div>
        </div>
    </div>
</div>
 *@